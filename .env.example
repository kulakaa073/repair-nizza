# Google My Business API Configuration
# Required for fetching Google reviews via /api/google-reviews
# 
# To get these credentials:
# 1. Go to Google Cloud Console (https://console.cloud.google.com/)
# 2. Create a new project or select an existing one
# 3. Enable the "Google My Business API"
# 4. Create OAuth 2.0 credentials (Web application)
# 5. Set authorized redirect URIs
# 6. Generate refresh token using OAuth 2.0 Playground or your own OAuth flow
# 7. Get your location ID from Google My Business dashboard
#
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here
GOOGLE_REFRESH_TOKEN=your_google_refresh_token_here
GOOGLE_MY_BUSINESS_LOCATION=locations/your_location_id_here

# Google Places API Configuration
# Required for fetching Google reviews via /api/google-reviews-places
# 
# To get these credentials:
# 1. Go to Google Cloud Console (https://console.cloud.google.com/)
# 2. Create a new project or select an existing one
# 3. Enable the "Places API" (New) or "Places API" (Legacy)
# 4. Create API key credentials
# 5. Get your Place ID from Google Maps (search for your business and get the Place ID)
# 
# Note: Places API is limited to 5 reviews maximum per request
#
GOOGLE_PLACES_API_KEY=your_google_places_api_key_here
GOOGLE_PLACE_ID=your_google_place_id_here

# Telegram Bot Configuration
# Required for sending messages via /api/send-message
#
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
TELEGRAM_CHAT_ID=your_telegram_chat_id_here
